import { Link } from 'react-router-dom';
import { products } from '../../data/products';
import { RemoteImage } from '../../components/ui/RemoteImage';
const stages = ['Order placed', 'In preparation', 'With carrier', 'Delivered'];
export function OrderTrackingPage() { return <main className="tracking-page container" aria-labelledby="tracking-title"><div className="flow-heading"><p className="eyebrow">Order / FX-1048</p><h1 id="tracking-title">Your order is in preparation.</h1><p>Estimated delivery 28 August 2026.</p></div><section className="tracking-timeline" aria-label="Order progress">{stages.map((stage, index) => <div className={index < 2 ? 'tracking-stage tracking-stage--active' : 'tracking-stage'} key={stage}><b>{String(index + 1).padStart(2, '0')}</b><span>{stage}</span></div>)}</section><section className="tracking-details"><div><p className="eyebrow">Shipment</p><h2>Atelier delivery</h2><p>Tracking updates will appear here when the order leaves the atelier.</p></div><div><p className="eyebrow">Your pieces</p>{products.slice(0, 1).map((product) => <div className="tracking-item" key={product.slug}><RemoteImage assetId={product.imageIds[0]} /><span>{product.name}</span></div>)}</div></section><Link className="text-link" to="/shop">Continue shopping -&gt;</Link></main>; }