import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Button } from '../../components/ui/Button';
const options = ['Quiet structure', 'Fluid ease', 'Avant-garde edge'];
export function OnboardingPage() { const [selected, setSelected] = useState(options[0]); const [complete, setComplete] = useState(false); return <main className="onboarding-page" aria-labelledby="onboarding-title"><div className="onboarding-header"><Link className="auth-wordmark" to="/">Falcon</Link><div className="onboarding-progress"><span /><span /><span /></div><p className="eyebrow">Step 1 / 3</p></div><section className="onboarding-content"><p className="eyebrow">Your visual language</p><h1 id="onboarding-title">Define your aesthetic.</h1><p>Select the style that resonates most. Falcon will use it to shape your digital atelier.</p><div className="selection-grid" role="radiogroup" aria-label="Aesthetic preferences">{options.map((option, index) => <button className={selected === option ? 'selection-card selection-card--selected' : 'selection-card'} key={option} type="button" role="radio" aria-checked={selected === option} onClick={() => setSelected(option)}><span>0{index + 1}</span><strong>{option}</strong><small>{index === 0 ? 'Precise and architectural' : index === 1 ? 'Softly considered and fluid' : 'Expressive and unexpected'}</small></button>)}</div>{complete && <p className="auth-success" role="status">Aesthetic saved: {selected}.</p>}<Button onClick={() => setComplete(true)}>Continue</Button></section></main>; }