import { Link } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { useCart, type CartItem } from '../../features/cart/CartContext';
export function ConfirmationPage() { const { items, clear } = useCart(); const [orderedItems] = useState<CartItem[]>(items); useEffect(() => { clear(); }, [clear]); return <main className="confirmation-page container" aria-labelledby="confirmation-title"><p className="eyebrow">Order received / FX-1048</p><h1 id="confirmation-title">Thank you for choosing your next piece.</h1><p className="confirmation-page__intro">Your order is being prepared by the atelier. A confirmation has been reserved for your records.</p><div className="confirmation-grid"><section><h2>Next steps</h2><p>We&apos;ll send delivery updates as your order moves through the atelier.</p><Link className="button button--secondary" to="/orders/FX-1048">View order tracking</Link></section><section><h2>Selected pieces</h2>{orderedItems.length ? orderedItems.map((item) => <p key={item.id}>{item.product.name} / {item.size}</p>) : <p>Your order details are available in your account.</p>}</section></div><Link className="text-link" to="/shop">Continue shopping -&gt;</Link></main>; }