import { AdminTable } from '../../components/admin/AdminTable';
import { supportTickets } from '../../data/admin';
const columns = [{ key: 'id', label: 'Ticket' }, { key: 'subject', label: 'Subject' }, { key: 'customer', label: 'Customer' }, { key: 'priority', label: 'Priority' }, { key: 'status', label: 'Status' }] as const;
export function AdminSupportPage() { return <div className="admin-page"><header className="admin-page-heading"><div><p className="eyebrow">Support center</p><h1>Care, considered.</h1><p>Resolve questions with the same attention as every atelier piece.</p></div><button className="button button--primary" type="button">New ticket</button></header><div className="admin-toolbar"><input aria-label="Search support tickets" placeholder="Search tickets" /><div className="filter-group"><button className="filter-chip filter-chip--active" type="button">All tickets</button><button className="filter-chip" type="button">Open</button><button className="filter-chip" type="button">High priority</button></div></div><AdminTable columns={columns as never} rows={supportTickets as never} /></div>; }