import { AdminMetric } from '../../components/admin/AdminMetric';
import { AdminTable } from '../../components/admin/AdminTable';
import { adminProducts } from '../../data/admin';
const columns = [{ key: 'sku', label: 'SKU' }, { key: 'name', label: 'Product' }, { key: 'stock', label: 'Available' }, { key: 'status', label: 'Signal' }, { key: 'category', label: 'Category' }] as const;
export function AdminInventoryPage() { return <div className="admin-page"><header className="admin-page-heading"><div><p className="eyebrow">Inventory management</p><h1>Stock intelligence.</h1><p>Keep the atelier ready for its next order.</p></div></header><section className="admin-metrics admin-metrics--compact"><AdminMetric label="Total units" value="1,842" detail="Across 84 SKUs" /><AdminMetric label="Low stock" value="06" detail="Needs review" tone="blue" /><AdminMetric label="Out of stock" value="02" detail="Replenishment needed" /></section><div className="admin-toolbar"><input aria-label="Search inventory" placeholder="Search SKU or product" /><div className="filter-group"><button className="filter-chip filter-chip--active" type="button">All stock</button><button className="filter-chip" type="button">Low stock</button><button className="filter-chip" type="button">Out of stock</button></div></div><AdminTable columns={columns as never} rows={adminProducts as never} /></div>; }