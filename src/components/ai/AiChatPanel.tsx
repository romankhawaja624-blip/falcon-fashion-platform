import { Send, Sparkles } from 'lucide-react';
import { useState } from 'react';
import { Link } from 'react-router-dom';
export function AiChatPanel({ builder = false }: { builder?: boolean }) { const [message, setMessage] = useState(''); const [sent, setSent] = useState(false); const submit = (event: React.FormEvent) => { event.preventDefault(); if (message.trim()) { setSent(true); setMessage(''); } }; return <section className="ai-chat-panel" aria-labelledby="ai-chat-title"><div className="ai-chat-panel__intro"><Sparkles size={34} aria-hidden="true" /><p className="eyebrow">Falcon AI</p><h1 id="ai-chat-title">{builder ? 'Build a look with intention.' : 'Good evening, Alex.'}</h1><p>{builder ? 'Choose an occasion, mood, or anchor piece. Falcon will compose the rest.' : 'What are we styling today? Describe an occasion, a mood, or a piece from your wardrobe.'}</p></div>{sent && <div className="ai-message" role="status"><span>You</span><p>I&apos;d like something considered for an evening in Milan.</p></div>}<div className="ai-suggestions"><button type="button" onClick={() => setMessage('A minimal evening look')}>Minimal evening look</button><button type="button" onClick={() => setMessage('Elevate my black silk pieces')}>Elevate my black silk pieces</button></div><form className="ai-composer" onSubmit={submit}><label className="sr-only" htmlFor="ai-prompt">Describe your desired aesthetic</label><textarea id="ai-prompt" value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Describe your desired aesthetic..." rows={1} /><button type="submit" aria-label="Send message"><Send size={18} aria-hidden="true" /></button></form>{builder && <Link className="text-link" to="/stylist/look/obsidian-evening">View a curated look -&gt;</Link>}</section>; }