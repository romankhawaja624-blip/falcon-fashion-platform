import { Minus, Plus, X } from 'lucide-react';
import { useCart, type CartItem } from '../../features/cart/CartContext';
import { RemoteImage } from '../ui/RemoteImage';

export function CartItemRow({ item }: { item: CartItem }) { const { updateQuantity, removeItem } = useCart(); return <article className="cart-item"><RemoteImage assetId={item.product.imageIds[0]} /><div className="cart-item__details"><p className="eyebrow">{item.product.category}</p><h2>{item.product.name}</h2><p>Size {item.size}</p><div className="quantity-control" aria-label={`Quantity for ${item.product.name}`}><button type="button" aria-label="Decrease quantity" onClick={() => updateQuantity(item.id, item.quantity - 1)}><Minus size={14} aria-hidden="true" /></button><span aria-live="polite">{item.quantity}</span><button type="button" aria-label="Increase quantity" onClick={() => updateQuantity(item.id, item.quantity + 1)}><Plus size={14} aria-hidden="true" /></button></div></div><strong className="cart-item__price">${(item.product.priceValue * item.quantity).toLocaleString()}</strong><button className="icon-button cart-item__remove" type="button" aria-label={`Remove ${item.product.name}`} onClick={() => removeItem(item.id)}><X size={17} aria-hidden="true" /></button></article>; }