import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useCheckout } from '../../features/checkout/CheckoutContext';
import { Button } from '../ui/Button';
export function PaymentForm() { const { payment, setPayment, setStep } = useCheckout(); const navigate = useNavigate(); const [error, setError] = useState(false); const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = Object.fromEntries(new FormData(event.currentTarget).entries()) as Record<string, string>; const valid = Object.values(data).every(Boolean); setError(!valid); if (valid) { setPayment(data); setStep('review'); navigate('/checkout/review'); } }; return <form className="checkout-form" onSubmit={submit} noValidate><p className="security-note">Secure payment details are used for this prototype only and are never stored.</p><div className="form-grid"><label className="form-field form-field--wide"><span>Cardholder name *</span><input name="cardholder" defaultValue={payment.cardholder} autoComplete="cc-name" required /></label><label className="form-field form-field--wide"><span>Card number *</span><input name="cardNumber" inputMode="numeric" autoComplete="cc-number" placeholder="4242 4242 4242 4242" required /></label><label className="form-field"><span>Expiration *</span><input name="expiration" inputMode="numeric" autoComplete="cc-exp" placeholder="MM / YY" required /></label><label className="form-field"><span>Security code *</span><input name="securityCode" inputMode="numeric" autoComplete="cc-csc" placeholder="CVC" required /></label></div>{error && <p className="form-error" role="alert">Please complete your payment details.</p>}<Button type="submit">Review order</Button></form>; }